### 📅 Notas Diárias - 2026-04-25

**1. Mercado e Cripto:**
- `crypto-swing-hunter` foi criado com foco em BTC, ETH, SOL, UNI, HYPE (via Bybit) e AAVE.
- Integra as teses de RSI, Volume de Absorção, Funding Rate e DVOL (Deribit Volatility Index).
- O robô de `swing-trade-hunter` (ações) foi aprimorado com rastreamento de `Smart Money` / `Insider Buying` (SEC Form 4 e fluxo de opções) para validação das oportunidades.
- `SJM` removida do portfólio; `BA` (2 ações) adicionada com PM ~$233.34.

**2. Segurança e Infraestrutura:**
- Skill `skill-audit` criada e implementada para triagem de segurança de novas skills.
- `backups/2026-04-21/openclaw.json` (backup com token exposto) foi apagado.
- `openclaw doctor --fix` foi executado, mas `apify` plugin segue com erro de carregamento (desatualizado).
- `openclaw-backup` (https://github.com/carlosdoriapm/openclaw-backup) configurado com `github_pat_` (fine-grained, Contents: Read and write) para backup diário dos arquivos do workspace (`MEMORY.md`, `USER.md`, `IDENTITY.md`, etc.) às 02:00h UTC.
- **⚠️ Problema pendente:** Credenciais do Google Drive/Workspace (email: `juliocesar.ops.coo.gmail.com`) via `gog` não autenticadas devido a problemas de `redirect_uri` em servidor headless. Tentativa de autorização via Client Secrets com `Desktop app` ainda não finalizada pelo usuário (requer intervenção manual no terminal local). Tokens de API (Telegram, OpenAI, etc.) continuam em texto puro no servidor, mas foi uma decisão assumida pelo usuário.

**3. Conteúdo e Automações:**
- `Relatório Diário de Inteligência de Mercado` configurado para usar a API da `Perplexity` (via OpenRouter) para pesquisa aprofundada, com entrega no Telegram às 08h São Paulo.
- `Relatório Institucional Premium Semanal` agendado para toda segunda-feira às 12h (Brasília), com gráficos (`matplotlib`) e design corporativo, enviado por e-mail para `carlosdoriapm@gmail.com`.
- Integração com `Zoho Mail` (`atendimento@zeluna.com.br`) finalizada com sucesso via Maton AI. Pode ler, rascunhar e enviar e-mails de SAC.
- `Máquina de Conteúdo` (Substack/Twitter -> Carrossel/Newsletter) configurada para Orange Book (Twitter) e Boss Letter (Substack, pendente URL pública ou bypass via e-mail).
- `Heartbeat de Gramado` configurado para avisar sobre temperatura < 10°C OU chuva às 08h, 14h, 20h (Brasília).